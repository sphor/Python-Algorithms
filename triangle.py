# INPUT Base value
# INPUT Height value
# Calculate area with formula 0.5*Base*Height
# PRINT The area is AREA square units.
# END.
base = float(input("Enter the base: "))
height = float(input("Enter the height: "))
area = 0.5 * base * height
print("The area is", area, "square units.")